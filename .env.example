# Frontend API base path (开发环境通常保持 /api，子路径部署可设为 /seedance/api)
VITE_API_BASE_URL=/api
# Frontend 站点基路径（根路径部署用 /，子路径部署如 /seedance/）
VITE_BASE_PATH=/
# Frontend 轮询超时时间（毫秒），默认 45 分钟
VITE_TASK_POLL_TIMEOUT_MS=2700000
# Default jimeng sessionid (optional, can be set in UI)
VITE_DEFAULT_SESSION_ID=
# Default xyq sessionid (optional, can be set in UI)
VITE_DEFAULT_XYQ_SESSION_ID=
# Express proxy port
PORT=3001
# 后端任务超时时间（毫秒），默认 45 分钟
VIDEO_GENERATION_TIMEOUT_MS=2700000
# 后端任务持久化文件（建议挂载到宿主机目录）
TASK_STORE_FILE=/app/data/tasks.json
# 已完成/失败任务保留时长（毫秒），默认 7 天
TASK_RESULT_TTL_MS=604800000
# 处理中任务保留时长（毫秒），默认 3 小时
TASK_PROCESSING_TTL_MS=10800000
# 任务落盘防抖时间（毫秒），默认 400ms
TASK_STORE_FLUSH_DELAY_MS=400
